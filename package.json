{
  "scripts": {
    "start": "run-p start:**",
    "build": "run-s build:**",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "postinstall": "bsb -make-world",
    "start:re": "BS_WATCH_CLEAR=true bsb -make-world -w",
    "clean": "bsb -clean-world",
    "start:snowpack": "snowpack dev",
    "build:re": "bsb -clean-world -make-world",
    "build:snowpack": "snowpack build"
  },
  "dependencies": {
    "@aws-amplify/api": "3.2.24",
    "@aws-amplify/core": "3.8.16",
    "@aws-amplify/pubsub": "3.2.22",
    "@rescript/react": "^0.10.1",
    "bootstrap": "4.6.0",
    "bs-platform": "^9.0.1",
    "react": "experimental",
    "react-dom": "experimental",
    "wonka": "5.0.0-rc.1"
  },
  "devDependencies": {
    "@reasonml-community/graphql-ppx": "^1.0.2",
    "@snowpack/plugin-react-refresh": "^2.4.0",
    "gentype": "^3.44.0",
    "npm-run-all": "^4.1.5",
    "patch-package": "^6.2.2",
    "snowpack": "^3.0.1"
  }
}
